#include "monty.h"
/**
 * f_stack -this prints the top
 * @head:it a stack head
 * @counter:this is line_number
 * Return: nothing return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
